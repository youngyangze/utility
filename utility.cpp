#include <bits/stdc++.h>

using namespace std;

namespace utility {
    template <typename T>
    int printVector(vector<T> vec, bool sorted = false) {
        if (vec.empty()) {
            return -1;
        } else {
            for (int i = 0; i < vec.size(); i++) {
                cout << vec[i] << '\n';
            }
        }
    }
}

int main() {


    return 0;
}
